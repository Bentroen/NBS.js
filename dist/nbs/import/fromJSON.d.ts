import { Song } from "../Song";
/**
 * Parse and return a {@linkcode Song} from a JSON object.
 *
 * @param json {@linkcode JSON} object to parse from
 * @returns Parsed song
 * @category Song
 * @category JSON
 */
export declare function fromJSON(json: object): Song;
